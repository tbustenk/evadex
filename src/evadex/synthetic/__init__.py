"""Synthetic value generation for dlpscan test patterns.

Each generator produces structurally valid values (correct checksums,
format constraints) so they exercise the scanner's detection logic
rather than just repeating a handful of seed strings.
"""
